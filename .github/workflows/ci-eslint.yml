name: ci-eslint
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: yarn global add eslint --prefix ~/bin
    - name: Run ESLint
      run: ~/bin/eslint . --ext .js,.jsx,.ts,.tsx
